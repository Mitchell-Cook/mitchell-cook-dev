# Installed to /etc/fail2ban/jail.local
# 5 failed SSH logins within 10m => ban that IP for 1h.
# backend=systemd reads the journal (correct for Ubuntu 24.04).
[DEFAULT]
bantime  = 1h
findtime = 10m
maxretry = 5
backend  = systemd

[sshd]
enabled = true
