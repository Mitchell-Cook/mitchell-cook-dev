# Installed to /etc/sysctl.d/99-swappiness.conf
# vm.swappiness=10       -> only swap when RAM is genuinely tight (default 60)
# vm.vfs_cache_pressure  -> keep filesystem metadata cached a bit longer
vm.swappiness=10
vm.vfs_cache_pressure=50
